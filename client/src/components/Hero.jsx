import { motion } from 'framer-motion';
import { HiArrowDown, HiSparkles } from 'react-icons/hi2';
const features = [['Structured, not chatty', 'Clean flashcards and MCQs from one focused prompt.'], ['Built for recall', 'Flip, quiz yourself, and repeat the gaps.'], ['Your pace, your space', 'A calm workspace that works beautifully everywhere.']];
export default function Hero({ onStart }) { return <section className="hero shell" id="top"><div className="blob blob-one"/><div className="blob blob-two"/><motion.div initial={{ opacity: 0, y: 16 }} animate={{ opacity: 1, y: 0 }} className="hero-copy"><p className="eyebrow"><HiSparkles /> Your private study studio</p><h1>Turn your notes into <em>smart flashcards.</em></h1><p className="lede">Luma turns dense material into a study set designed for the moments you need it most: right before it clicks.</p><button className="primary-button" onClick={onStart}>Create a study set <HiArrowDown /></button></motion.div><div className="feature-grid">{features.map(([title, text], index) => <motion.article key={title} className="feature-card" initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: .15 + index * .1 }}><span>0{index + 1}</span><h3>{title}</h3><p>{text}</p></motion.article>)}</div></section>; }
